# Frontend local development settings

# true = no backend, everything mocked
VITE_USE_MOCK=false

# Local backend URL for Vite dev
# In production, frontend uses same-origin requests through NGINX,
# so this variable is normally not needed there.
VITE_API_BASE=http://localhost:3001
